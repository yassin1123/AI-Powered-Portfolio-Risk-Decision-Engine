# Research

Place **notebooks**, **ablation / walk-forward CSVs**, and **figures** here.

Suggested layout (create as you run experiments):

```text
research/
  notebooks/
    ablation_study.ipynb
    walkforward.ipynb
    anomaly_predictive_value.ipynb
  ablation_results.csv
  walkforward_results.csv
  figures/
```

Commit **results CSVs** and **exported figures** when stable; keep huge raw outputs gitignored (see root `.gitignore`).
